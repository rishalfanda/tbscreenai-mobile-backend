# Web framework
fastapi~=0.116.0
uvicorn[standard]~=0.35.0
python-multipart~=0.0.20

# Database
sqlalchemy~=2.0.41
alembic~=1.16.0
psycopg[binary]~=3.2.9

# Config & validation
pydantic~=2.11.0
pydantic-settings~=2.10.0
email-validator~=2.2.0

# Auth
PyJWT~=2.10.1
bcrypt~=4.3.0

# Testing (FASE 5, installed now so the stack is pinned together)
pytest~=8.4.0
httpx~=0.28.1
pytest-cov~=7.1.0

# Quality gates — run by CI (.github/workflows/backend.yml).
# Both are configured in pyproject.toml.
ruff~=0.16.0
mypy~=2.3.0
