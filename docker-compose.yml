services:
  db:
    image: postgres:16
    container_name: tbscreenai-db
    environment:
      POSTGRES_USER: tbscreen
      POSTGRES_PASSWORD: tbscreen_dev
      POSTGRES_DB: tbscreenai
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tbscreen -d tbscreenai"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
